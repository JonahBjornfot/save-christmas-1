function max() {
    let arr = Math.max(10, 20, 30, 40);
    console.log(arr)
}

max();

function min() {
    let arr = Math.min(10, 20, 30, 40);
    console.log(arr)
}

min();

function mean() {
    let arr = (10 + 20 + 30 + 40) / 4
    console.log(arr)
}

mean();

function name(arr) {
    for (i = 0; i < arr.lenght; i++)
    if (arr[i]) {
        
    }
}